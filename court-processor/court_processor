#!/usr/bin/env python3
"""
Backward compatibility wrapper for court_processor CLI
Redirects to the new cli.py
"""
import sys
import os

# Add the directory to Python path
sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))

# Import and run the CLI
from cli import cli

if __name__ == '__main__':
    cli()